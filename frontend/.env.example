# 复制为 .env 生效（.env 本身不进 Git）

# ---- 数据来源开关 ----
# true / 缺省 = 使用前端 mock 数据（无需后端，可离线演示）
# false      = 走真实 FastAPI 后端（需先启动 backend: uvicorn main:app --port 8000）
VITE_USE_MOCK=true

# ---- 天地图 Key（可选）----
# 申请：https://console.tianditu.gov.cn/
# 未配置时自动回退 OSM 底图，不影响演示
#
# ⚠️ 建议填在 `.env.vps.local` 而非本文件：该文件已被 .gitignore 忽略，
#    Key 不会随仓库上传（`.env.vps` 是被追踪的，Key 会进 GitHub）。
#    加载优先级：.env.vps.local > .env.vps > .env.local > .env
#    改完 Key 必须重新执行 `npm run build:vps`，因为它是编译期变量。
# VITE_TIANDITU_KEY=your_key_here

# ---- 密钥保管原则 ----
# 前端产物（dist）是公开 JS，凡 VITE_ 前缀的变量都会被打包并可被任意访客读取。
# 因此：天地图 Key 放前端（浏览器直连天地图，本就公开，注意防盗刷配额）；
#       大模型 Key（如 DeepSeek）必须放后端 backend/.env，绝不能写进前端。

# ---- VPS 部署构建 ----
# 部署到服务器用 `npm run build:vps`，该命令读取 .env.vps（VITE_USE_MOCK=false），
# 与本地默认构建互不影响。详见 deploy/vps/DEPLOY.md。
