# 复制为 backend/.env 后填写。.env 本身不进 Git。

# ---- 服务 ----
API_HOST=127.0.0.1
API_PORT=8000

# ---- AI 增强（可选；关闭时系统必须仍可跑通全流程）----
LLM_ENABLED=false
# LLM_BASE_URL=
# LLM_API_KEY=
# LLM_MODEL=

# ---- 选址引擎默认参数 ----
DEFAULT_GRID_SIZE_M=30
DEFAULT_MIN_AREA_HA=1.0
DEFAULT_TOP_N=5
