# 核心依赖（小型本地数据库路线，不引 Docker / PostGIS）
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0

# 空间分析
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
pyogrio>=0.8            # GeoPackage / Shapefile 快速读取（geopandas 引擎）
numpy>=1.26
pandas>=2.1

# 工具
python-multipart>=0.0.9
orjson>=3.9
httpx>=0.27             # 大模型调用（LLM 可选增强）与接口自测

# 可选：栅格分析（当前引擎未使用，保留以备坡度/坡向因子扩展）
# rasterio>=1.3
